# Alacritty palettes

These palettes come from [alacritty/alacritty-theme](https://github.com/alacritty/alacritty-theme).
See upstream for previews and documentation; this copy omits Git metadata and images.

`alacritty-tools` includes `themes/*.toml` here in its theme picker. Keep these
palettes alongside its own palettes in `../colors/`. The upstream license is
preserved in `LICENSE`.
