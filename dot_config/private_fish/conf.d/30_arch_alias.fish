# Arch linux specific aliases
alias pacman "sudo pacman"
alias zfsmochi='zfs list -r -o name,mountpoint,used,avail,compressratio,compression,atime,exec kura'
alias kura="cd /media ; cat kura.txt"
alias ls=lsd
alias cat=bat
