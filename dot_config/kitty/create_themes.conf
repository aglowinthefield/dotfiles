# Written by `kitten themes --config-file-name themes.conf`.
#
# Kept separate so the kitten never edits kitty.conf. Empty until a theme is
# picked that way; the dark-theme.auto.conf / light-theme.auto.conf pair
# overrides anything landing here.
